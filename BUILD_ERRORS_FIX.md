# 🔧 Виправлення помилок компіляції

## ✅ Вже виправлено:

1. ✅ `movie_card.dart` - закриті дужки для Hero widget
2. ✅ `app_database.dart` - popularity змінено на TextColumn
3. ✅ `movies_local_datasource.dart` - додано genreIds
4. ✅ `main.dart` - додано імпорт для CheckAuthStatusEvent
5. ✅ `widget_test.dart` - оновлено тест

## ⚠️ Потрібно виконати:

### 1. Згенерувати Drift database код

**Команда:**
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

**Що це зробить:**
- Створить `app_database.g.dart` з типами `MoviesData`, `FavoriteData`
- Згенерує всі необхідні методи

**Після генерації:**
- Типи `MoviesData` будуть доступні
- Всі помилки з типами зникнуть

### 2. Запустити тести знову

```bash
flutter test --coverage
```

---

## 📋 Поточний статус:

- **Тести, що працюють:** 15/17 ✅
- **Помилки компіляції:** 
  - Database types (вирішиться після build_runner)
  - Generated code issues (вирішиться після build_runner)

---

## 🎯 План дій:

1. **Спочатку** запустити `build_runner`:
   ```bash
   flutter pub run build_runner build --delete-conflicting-outputs
   ```

2. **Потім** запустити тести:
   ```bash
   flutter test --coverage
   ```

3. Перевірити coverage звіт

---

## 💡 Примітки:

- `MoviesData` - це тип, який генерується Drift після build_runner
- До генерації код може не компілюватися
- Після генерації все має працювати автоматично

