# 🖥️ Команди для терміналу

## 📊 Статус тестів:

**Успішно пройдено:** 15 тестів ✅

## ⚠️ Помилки компіляції (виправляться після build_runner):

1. Database types - потрібна регенерація
2. Generated code - потрібна регенерація

---

## 🔧 Виконайте в такому порядку:

### Крок 1: Очистити попередні згенеровані файли
```bash
flutter pub run build_runner clean
```

### Крок 2: Згенерувати код заново
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

**Це займе 30-60 секунд.**

### Крок 3: Запустити тести з coverage
```bash
flutter test --coverage
```

### Крок 4: Перевірити coverage звіт
```bash
# Coverage файл: coverage/lcov.info
```

---

## ✅ Результати (до виправлення):

- ✅ **15 тестів пройшли успішно**
- ✅ **Auth tests** - працюють
- ✅ **Movies BLoC tests** - працюють  
- ✅ **Use cases tests** - працюють

---

## 📝 Після виконання build_runner:

1. Всі типи Drift будуть згенеровані
2. `MoviesData` буде доступний
3. Всі помилки компіляції зникнуть
4. Coverage збільшиться до ~70%+

---

## 💡 Поради:

- Якщо build_runner падає - перезапустіть
- Перевірте, що всі залежності встановлені: `flutter pub get`
- Coverage report генерується автоматично в `coverage/`

