# 🚀 Інструкції для запуску додатку

## ✅ Стан системи:
- ✅ Flutter встановлено та працює
- ✅ Android емулятор доступний (`emulator-5554`)
- ✅ Windows desktop доступний
- ✅ Chrome/Edge доступні для web

---

## 📱 Варіанти запуску:

### 1. Запуск на Android емуляторі (рекомендовано):
```bash
flutter run
```
або
```bash
flutter run -d emulator-5554
```

### 2. Запуск на Windows desktop:
```bash
flutter run -d windows
```

### 3. Запуск у браузері (Chrome):
```bash
flutter run -d chrome
```

---

## ⚠️ Важливо перед запуском:

### 1. Перевірити .env файл:
Переконайтеся, що у корені проекту є файл `.env` з вашим TMDB API ключем:
```
TMDB_API_KEY=your_actual_api_key_here
```

Якщо файлу немає - створіть його!

### 2. Переконатися, що build_runner виконано:
```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. Оновити залежності (якщо потрібно):
```bash
flutter pub get
```

---

## 🎯 Рекомендований порядок дій:

1. **Перевірити .env файл** з API ключем
2. **Запустити емулятор** (якщо ще не запущений)
3. **Запустити додаток:**
   ```bash
   flutter run
   ```

---

## 🔧 Якщо виникли проблеми:

### Помилка: "No devices found"
```bash
# Запустити емулятор
flutter emulators --launch <emulator_id>
```

### Помилка: "API key not found"
- Перевірте наявність `.env` файлу
- Переконайтеся що він у корені проекту
- Перевірте правильність назви: `TMDB_API_KEY=...`

### Помилка: "Database not found"
- Запустіть build_runner:
  ```bash
  flutter pub run build_runner build --delete-conflicting-outputs
  ```

---

## ✨ Після запуску:

Додаток має:
- ✅ Показати HomePage з фільмами
- ✅ Дозволити авторизацію (Login/Register)
- ✅ Показувати деталі фільмів
- ✅ Працювати з пошуком

**Готово до використання!** 🎬

